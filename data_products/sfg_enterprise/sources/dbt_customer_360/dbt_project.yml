name: sfg_enterprise_customer_360
version: '1.0.0'

profile: sfg_enterprise_customer_360

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  sfg_enterprise_customer_360:
    marts:
      +schema: MART_CUSTOMER_360
      +materialized: table
