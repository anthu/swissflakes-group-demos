version: 2

models:
  - name: sfg_logistics_shipments
    description: >
      Organization listing that shares shipment tracking data with all
      accounts in the organization.
    config:
      materialized: organization_listing
      meta:
        share_name: SFG_LOGISTICS_SHIPMENTS
        listing_name: SFG_LOGISTICS_SHIPMENTS
        publish: false
        listing_manifest:
          title: "SwissFlakes Logistics -- Shipments"
          description: |
            Shipment data product from SwissFlakes Logistics AG.

            **Data Tables:**
            - **MART_SHIPMENT_TRACKING** -- shipment lifecycle with tracking events
          organization_profile: "INTERNAL"
          organization_targets:
            access:
              - all_internal_accounts: true
          locations:
            access_regions:
              - name: "ALL"
          auto_fulfillment:
            refresh_type: "SUB_DATABASE"
            refresh_schedule: "10 MINUTE"
